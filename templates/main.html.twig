{# app/Resources/views/user/login.html.twig #}
{% extends 'base.html.twig' %}


{% block body %}
    <header>
        <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark d-flex">
            <div class="p-2" id="navbarCollapse">
                <ul class="navbar-nav">
                    <li class="nav-item"><a class="nav-link" href="/">{% trans %}Expenses{% endtrans %}</a></li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="dropdown01" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">{% trans %}Lang{% endtrans %}</a>
                        <div class="dropdown-menu" aria-labelledby="dropdown01">
                            <a class="dropdown-item" href="{{ path(app.request.get('_route'), app.request.get('_route_params')|merge({'_locale': 'lv'})) }}">LV</a>
                            <a class="dropdown-item" href="{{ path(app.request.get('_route'), app.request.get('_route_params')|merge({'_locale': 'en'}))}}">EN</a>
                            <a class="dropdown-item" href="{{ path(app.request.get('_route'), app.request.get('_route_params')|merge({'_locale': 'ru'}))}}">RU</a>
                        </div>
                    </li>
                    {#<li class="nav-item"><a class="nav-link" href="{{ logout_path('main') }}"><span class="fas fa-lock" aria-hidden="true"></span> {% trans %}Logout{% endtrans %}</a></li>#}
                    <li class="nav-item user_info">

                    </li>
                </ul>
            </div>

        </nav>
    </header>
    <div class="container-fluid">
        <div class="row">

            <div class="col-sm-12 col-md-12">

                {% block content %}{% endblock %}

            </div>
        </div>
    </div>

{% endblock %}